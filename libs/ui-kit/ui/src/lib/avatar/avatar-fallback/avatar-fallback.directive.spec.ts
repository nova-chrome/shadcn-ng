import { AvatarFallbackDirective } from './avatar-fallback.directive';

describe('AvatarFallbackDirective', () => {
  it('should create an instance', () => {
    const directive = new AvatarFallbackDirective();
    expect(directive).toBeTruthy();
  });
});
